version: "3.8"

services:
  touchorbit-admin-web:
    build:
      context: ../..
      dockerfile: infra/touchorbit-admin-web/Dockerfile
    image: cloudit/touchorbit-admin-web:latest
    container_name: touchorbit-admin-web
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    env_file:
      - ../../apps/touchorbit-admin-web/.env
    environment:
      - NODE_ENV=production
      - PORT=3008
    networks:
      - cloudit
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.touchorbit-admin-web.rule=Host(`to-admin.${DOMAIN:-cloudit.lk}`)"
      - "traefik.http.routers.touchorbit-admin-web.entrypoints=websecure"
      - "traefik.http.routers.touchorbit-admin-web.tls.certresolver=cloudflare"
      - "traefik.http.routers.touchorbit-admin-web.tls.domains[0].main=${DOMAIN:-cloudit.lk}"
      - "traefik.http.routers.touchorbit-admin-web.tls.domains[0].sans=*.${DOMAIN:-cloudit.lk}"
      - "traefik.http.services.touchorbit-admin-web.loadbalancer.server.port=3008"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:3008 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.1'
          memory: 128M

networks:
  cloudit:
    external: true
