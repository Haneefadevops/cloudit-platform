version: "3.8"

# This file creates the shared external network that all services connect to.
# Run this first: docker-compose -f docker-compose.network.yml up -d

networks:
  cloudit:
    driver: bridge
    name: cloudit
    attachable: true
