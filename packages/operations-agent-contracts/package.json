{
  "name": "@cloudit/operations-agent-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "CloudIT AI Maintenance - offline contracts, canonicalization and security fixtures (Programme Phase B)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.5"
  }
}
