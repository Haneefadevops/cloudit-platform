version: "3.8"

services:
  hospitality-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hospitality-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3002
      DATABASE_URL: ${DATABASE_URL}
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
    networks:
      - cloudit
    ports:
      - "3002:3002"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.hospitality-api.rule=Host(`api-hospitality.cloudit.lk`)"
      - "traefik.http.routers.hospitality-api.entrypoints=websecure"
      - "traefik.http.routers.hospitality-api.tls.certresolver=cloudflare"
      - "traefik.http.services.hospitality-api.loadbalancer.server.port=3002"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3002/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M

networks:
  cloudit:
    external: true
