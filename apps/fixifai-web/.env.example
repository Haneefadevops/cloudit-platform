# SMTP settings for pilot signup email notifications.
# Copy this file to .env on the SERVER (same directory) and fill real values.
# .env is gitignored — never commit credentials.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=fixifai@cloudit.lk
SMTP_PASS=replace-me
NOTIFY_TO=fixifai@cloudit.lk
