// Summaries/answers service — public API (Worker B, AI-brain phase).
// Exported shapes are contractual: Worker C writes evals against them blind.
export {
  GetExplanationInput,
  GetExplanationResult,
  SummariesServiceOptions,
  SummariesService,
} from './summaries.service';
